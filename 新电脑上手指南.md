# 新电脑上手指南

> 关于仓库里散落的**别人的电脑路径**（`/home/simon/...`、`/home/dawntildusk/...`、
> `/home/lionheart/...`），单独有一份说明：**《电脑路径说明.md》**，建议先读那份。

**适用前提**：机械臂、相机、灵巧手、夹具、桌面摆放与本机**严格相同**，只有电脑不同。
因此代码、标定、示教序列都能直接复用，**只有"电脑环境"需要在新电脑上重建**。

核对日期：2026-09-16。

## 0. 哪些直接复用，哪些必须重建

| 内容 | 换电脑后 | 原因 |
| --- | --- | --- |
| `tools/` 下的程序 | ✅ 直接用 | 与机器无关 |
| `开发资源/calibration/*.yaml` | ✅ 直接用 | 相机外参取决于相机**安装几何**，与电脑无关 |
| `recordings/*/events.jsonl` | ✅ 直接用 | 示教序列：ready 段、中央释放点、入盒点 |
| `开发资源/nut_sort/nut_task.yaml` 的抓取参数 | ✅ 直接用 | 视觉相对偏移、手型，与电脑无关 |
| `weights/*.pt` | ✅ 直接用 | 模型权重已在仓库，**不需要下载** |
| `.venv-yolo/` | ❌ 每台重建 | venv 内写死绝对路径，不能拷贝 |
| `build/` `install/` `log/` | ❌ 每台重建 | colcon 产物，绝对路径写死 |
| 相机驱动 `orbbec_ws` | ❌ 每台准备 | 独立工作区，不在本仓库内 |
| 驱动动态库、网卡 IP | ❌ 每台配置 | 见第 3、6 节 |

## 1. 系统要求

Ubuntu 24.04 + ROS 2 Jazzy + Python 3.12。

```bash
sudo apt install -y ros-jazzy-desktop python3-pip python3-venv
```

系统 Python（`/usr/bin/python3`）需要：`rclpy`、`cv2`、`numpy`、`scipy`、`PIL`。
ROS 程序一律用 `/usr/bin/python3` 跑，YOLO 推理走独立环境（第 4 节）。

## 2. 编译本工作区

```bash
cd <工作区根>
source /opt/ros/jazzy/setup.bash
colcon build
source install/setup.bash
```

> **必须做。** `tools/` 里的抓取程序依赖 `lbot_arm_interfaces` 的自定义服务；
> 不编译会报 `ModuleNotFoundError: No module named 'lbot_arm_interfaces'`。

## 3. 安装驱动动态库（需要 sudo）

```bash
cd <工作区根>/src/lbot_driver/lib
sudo ./lib_install.sh
```

## 4. 建 YOLO 推理环境

`nut_task.yaml` 里是 `detector.python: auto`，会**先找工作区内的 `.venv-yolo`**，
其次找 `~/miniconda3/envs/nut-yolo`。所以建在工作区根目录最省事：

```bash
cd <工作区根>
/usr/bin/python3 -m venv .venv-yolo
.venv-yolo/bin/pip install --upgrade pip

# 有 NVIDIA 显卡（RTX 50 系 / Blackwell 必须用 cu128 及以上，默认 PyPI 的 torch 不支持）
.venv-yolo/bin/pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# 纯 CPU（体积小，先跑通够用）
# .venv-yolo/bin/pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

.venv-yolo/bin/pip install ultralytics
```

自检：

```bash
.venv-yolo/bin/python -c "import torch, ultralytics; print(torch.__version__, torch.cuda.is_available())"
```

## 5. 相机驱动（Orbbec Gemini2）

相机驱动是独立工作区，不在本仓库内。已有就直接 source，没有就从 OrbbecSDK_ROS2 编译一份。

**启动命令（`time_domain:=system` 不能省，原因见第 7.1 节）：**

```bash
source /opt/ros/jazzy/setup.bash
source <orbbec_ws>/install/setup.bash
ros2 launch orbbec_camera gemini2.launch.py \
  enable_color:=true enable_depth:=true depth_registration:=true \
  enable_point_cloud:=false enable_colored_point_cloud:=false \
  time_domain:=system
```

## 6. 网络与机械臂驱动

控制器默认 IP `192.168.10.21`。电脑网卡配到同网段（如 `192.168.10.3/24`），确认 `ping 192.168.10.21` 通。

机械臂驱动建议开一个常驻终端：

```bash
source /opt/ros/jazzy/setup.bash
source <工作区根>/install/setup.bash
ros2 launch lbot_driver lbot_start_driver.launch.py
```

## 7. 验证（按顺序，别跳）

```bash
# 每个新终端都要先 source 这三行
source /opt/ros/jazzy/setup.bash
source <工作区根>/install/setup.bash
source <orbbec_ws>/install/setup.bash

# ① 节点都在
ros2 node list
# 期望看到 /robot1/lbot_left_arm_node、/robot1/lbot_right_arm_node、
#          /robot1/lbot_main_node、/camera/camera

# ② 视觉通路（只读，不动机械臂）
cd <工作区根>
/usr/bin/python3 tools/nut_yolo_live.py --model weights/nut_white_best.pt --device cpu --conf 0.5

# ③ 干跑抓取流程（打印计划，不动机械臂）
/usr/bin/python3 tools/nut_pick_place.py --detector yolo

# ④ 确认无误后再真机
/usr/bin/python3 tools/nut_pick_place.py --detector yolo --execute --speed 0.5
```

## 8. 已知坑（都实际踩过）

### 8.1 相机时间戳必须用 `time_domain:=system`

`gemini2.launch.py` 默认 `time_domain:=global`，依赖"设备全局时钟 ↔ 主机时钟"同步。
本硬件上这个同步会坏，表现为**彩色与深度的时间戳差几十到几百秒**，抓取程序报：

```
[中止] 等待彩色、对齐深度、彩色 camera_info 超时，或图像时间戳不同步
```

加 `time_domain:=system` 后改用主机收帧时间，两路共用主机时钟，不会再偏。

### 8.2 `ros2 topic hz` 显示的帧率不可信

图像大时 `ros2 topic hz` 自己跟不上，可能显示 2~5 Hz 甚至 5 秒空档，
实际是满帧 ~30 Hz。要准就写个轻量订阅脚本数帧。

### 8.3 厂商 SDK 的 IK 接口会失败（左臂已绕过）

`lbot_move_pose`(MoveJP) 与 `lbot_move_linear`(MoveL) 都在 SDK 内部自己做 IK，
**不接受初值**，失败时只回一个**空错误串**。

左臂视觉抓取的 **中转横移 / 下降到 hover / 下探 down / 上抬 lift** 已改成
「外部多种子 IK + MoveJ」：我们用驱动的 IK 服务解出关节角，SDK 只收关节角执行。
**竖直上升到中转平面仍用原生 MoveL**（纯竖直，从未失败）。

若以后其他步骤也报 `move_pose 失败/超时` 或 `move_linear 失败/超时`，
可套用 `tools/nut_pick_place.py` 里的 `move_via_ik()`。

### 8.4 IK 请求绝不能发空 `joints`

`InverseKinematics.srv` 注释说"不设置会默认从机械臂读取当前角度"，但驱动实现是
`initial_ptr = nullptr`，而 SDK 的 `lbot_create_ik_request` 会解引用它 ——
**空指针，整个驱动进程段错误退出**。`nut_robot.py` 的 `ik_try` / `ik_check`
已强制不发空数组，改代码时别退回。

### 8.5 `hand.open` 必填

缺省会误发全 0，把手闭合。

### 8.6 灵巧手 6 路顺序（与 URDF 关节名相反）

```
[拇指弯曲, 拇指侧摆, 食指, 中指, 无名指, 小指]
```
255 = 完全张开，0 = 完全闭合。详见 `灵巧手说明.md`。

## 9. 换机后的标定核验（建议做一次）

硬件虽相同，仍建议确认相机支架没有装配偏差：

```bash
cd <工作区根>
/usr/bin/python3 tools/calib_handeye_verify.py
```

在线核验需要把 ChArUco 板放进视野（`开发资源/calibration/charuco_board.png` 可打印）。
两条独立路径（视觉 / 运动学）算出的板原点差值在**数毫米内**即正常；
偏差很大的话再考虑重标。
