# 电脑路径说明（换电脑 / 新人必读）

> **一句话**：本仓库的**代码、标定、示教序列与电脑无关**，可以直接复用。
> 但仓库里散落着**至少 4 台不同电脑**的绝对路径 —— 那些是各人本机的操作记录，
> 照着敲会报错。本文把它们全部列出来，说明每一条怎么换算。

核对日期：2026-09-16。

---

## 0. 先弄清「工作区根」是什么

本文所有 `<工作区根>` 指**这个仓库的根目录** —— 也就是你 clone / 解压出来、
里面有 `tools/`、`开发资源/`、`src/`、`weights/` 的那个目录。

```bash
cd <工作区根>
ls    # 应该看到：tools  开发资源  src  weights  README.md  ...
```

本机上它是 `/home/undefined/lbot_ws-2`。**你的可能不同，这不影响使用** ——
程序内部用 `Path(__file__).resolve().parents[1]` 自己算工作区根，
不依赖任何写死的绝对路径。

---

## 1. 仓库里出现过哪些「别人的路径」

| 路径前缀 | 属于谁 | 出现在哪些文件 |
| --- | --- | --- |
| `/home/simon/lbot_ws` | 原始 SDK 文档作者 | `开发环境说明.md`(4) · `README.md`(4) · `相机与视觉传感器说明.md`(5) · `数据备份说明.md`(4) · `现场快速检查表.md`(1) |
| `/home/dawntildusk/lbot_ws`、`/home/dawntildusk/nut_vision`、`/home/dawntildusk/miniconda3`、`/home/dawntildusk/orbbec_ws` | 队友 dawntildusk（视觉 / 训练） | `视觉模块指令手册.md`(20+) · `tools/NUT_TASK.md`(3) |
| `/home/lionheart/Project/1_Competition/THUEI/...` | 队友 lionheart | `tools/CALIBRATION.md`(2) · `tools/NUT_TASK.md`(2) · `tools/nut_detector_example.py`(1) |
| `/home/ran/lbot_ws` | 队友 ran | 已修掉（原在 `nut_task.yaml` 的 `detector.external`） |

**这些文档没有改** —— 它们是各人本机的操作记录，改了就失真。
读的时候请自行换算：把上面前缀替换成你的 `<工作区根>`。

> **例外：`weights/*.md` 里的路径不要动。** 那是「某次训练在哪台机器上跑的」
> 来源记录，属于历史事实。

---

## 2. 已经修掉的可移植性问题（逐项说明）

这一节说明**具体改了什么、为什么**。想直接看结论的跳到 2.5。

### 2.1 `nut_task.yaml` 的 `detector.external`：去掉别人机器的绝对路径

**这是什么**：告诉程序「你自己写的检测器文件在哪」。只有把
`detector.type` 改成 `external`（换成自己的识别算法）时才会读它。

**改之前**：

```yaml
external: '/home/ran/lbot_ws/开发资源/nut_sort/nut_vision_detector.py:NutVisionDetector'
#          ^^^^^^^^^^^^^^ 队友 ran 的电脑，别人机器上不存在
```

后果：一旦切到 external，程序报「外部检测器文件不存在」。

**改之后**：

```yaml
# 格式 '文件路径或模块:类名'；相对路径按【工作区根目录】解析。
# 指向仓库自带的示例检测器；换自己的工作区/自己的检测器时改这里。
external: '开发资源/nut_sort/nut_vision_detector.py:NutVisionDetector'
```

**同时改了一行代码**（`tools/nut_detectors.py`）：原来相对路径是按
**当前所在目录**解析的 —— 意味着你只有在工作区根目录下运行才对。
现在改成**先按工作区根找**，找不到再退回当前目录，所以在哪个目录运行都行。

### 2.2 `capture_nut_images.py` 的 `--output`：默认目录移进工作区

**这是什么**：采图工具（拍照片做训练数据）的默认输出目录。

**改之前**：

```python
default=Path('/home/dawntildusk/nut_vision')   # 队友的文件夹，别人机器上不存在
```

后果：别人直接运行（不传 `--output`），照片会往不存在的目录写，报错。

**改之后**：

```python
WORKSPACE = Path(__file__).resolve().parents[1]   # 自动算出工作区根
default=WORKSPACE / 'captures'                    # 存到自己工作区里
```

想存别处仍然可以 `--output` 指定。

### 2.3 `.gitignore`：拦住调参产生的备份文件

**这是什么**：告诉 git 「这些文件不要上传」的清单。

**问题**：现场调参时每改一次配置/录制都会留一个 `.bak_*` 备份，
本仓库累积了 **113 个**。不拦的话 `git push` 会把它们全传出去，
仓库变大，别人拉下来会看到一堆无关的历史副本。

**改动**：

```diff
  .venv-yolo/
+
+ # 现场调参时自动产生的文件备份（tools/、recordings/、开发资源/ 下都有）
+ *.bak_*
+ # capture_nut_images.py 的默认采图输出目录
+ captures/
```

### 2.4 新增《新电脑上手指南.md》

解决「**别人拿到这个文件夹后不知道要做什么**」。内容四块：

1. **哪些能直接复用，哪些必须重建**（一张表）
2. **具体步骤**：装依赖 → `colcon build` → 装驱动库 → 建 YOLO 环境 → 起相机 → 配网络
3. **验证顺序**（先验证再动机器人，别跳）
4. **8 个已知坑**：全是现场实际踩过的（相机 `time_domain:=system`、
   `ros2 topic hz` 帧率显示不可信、IK 空种子会让驱动段错误退出等）

### 2.5 汇总：程序实际会读的路径，现状

| 位置 | 现状 |
| --- | --- |
| `nut_task.yaml` → `detector.python` | `auto`：先找 `<工作区根>/.venv-yolo`，再找 `~/miniconda3/envs/nut-yolo`，都没有才报错 |
| `nut_task.yaml` → `detector.external` | 工作区相对路径；加载时**先按工作区根解析**，再退回当前目录 |
| `capture_nut_images.py` → `--output` | 默认 `<工作区根>/captures` |

其余配置、权重、录制文件里的路径**都是相对工作区**的，天然可移植。

> **判断标准**：路径只要不以 `/` 开头，或者以 `Path(__file__)` 推算出来，
> 就与电脑无关。以 `/home/` 开头的才需要警惕。

---

## 3. 必须每台电脑重建的东西（不能拷贝）

| 内容 | 为什么不能拷 | 怎么建 |
| --- | --- | --- |
| `.venv-yolo/` | venv 内部写死了绝对路径 | 见《新电脑上手指南.md》第 4 节 |
| `build/` `install/` `log/` | colcon 产物，绝对路径写死 | `colcon build` |
| 相机驱动 `orbbec_ws` | 独立工作区，不在本仓库内 | 自行编译，或使用已有的 |
| 驱动动态库 | 要装到系统目录 | `sudo src/lbot_driver/lib/lib_install.sh` |
| 网卡 IP | 网络配置 | 配到 `192.168.10.x`，能 ping 通 `192.168.10.21` |

前三项已在 `.gitignore` 里排除，**不会随仓库分发**。

---

## 4. 给「用 AI 助手帮你跑」的人

把下面这段直接交给你的 AI：

> 这是一个 ROS 2 Jazzy 的机械臂螺母抓放项目，当前工作区根目录是 `<你的路径>`。
> 请先读 `电脑路径说明.md` 和 `新电脑上手指南.md`。
>
> 仓库里其他文档出现的 `/home/simon/...`、`/home/dawntildusk/...`、
> `/home/lionheart/...` 都是**别的电脑**的路径，执行命令时请替换成当前工作区根；
> 但**不要修改那些文档**，也不要改 `weights/*.md` 里的训练来源路径。
>
> 程序本身不依赖这些绝对路径。如果某个程序报「找不到文件」，
> 先确认 `<工作区根>/<相对路径>` 存在，再确认有没有 `source` 三个环境：
> `/opt/ros/jazzy/setup.bash`、`<工作区根>/install/setup.bash`、
> `<orbbec_ws>/install/setup.bash`。

---

## 5. 自查：你机器上还有没有残留的硬编码路径

```bash
cd <工作区根>
grep -rnE "/home/(simon|dawntildusk|lionheart|ran)/" \
  --include=*.py --include=*.yaml . | grep -v "\.bak" | grep -v "\.venv-yolo"
```

**正常情况**：输出为空，或只出现在 `.md` 文档里。

如果出现在 `.py` / `.yaml` 的**可执行代码**里，那就是需要修的 bug ——
请反馈，不要自己硬改（很可能是别人机器专用的调试残留）。

---

## 6. 一句话总结

- **能跟着仓库走的**：程序、标定、示教序列、模型权重 → 直接复用
- **每台电脑要重建的**：venv、colcon 产物、相机驱动、驱动库、网络
- **要换算的**：文档里的 `/home/xxx/...` → 你的 `<工作区根>`
- **不要动的**：`weights/*.md` 里的训练来源路径
