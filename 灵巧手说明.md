# 灵巧手说明

## 使用哪套接口

本目录里的 `lbot_driver` 已经提供灵巧手 ROS2 话题，不需要参赛队直接调用底层 C/C++ API。

O6 手是 6 个主动关节，所以按 L6 话题来发控制值：

```text
/robot1/left_hand/set_l6_joint
/robot1/left_hand/set_l6_speed
/robot1/left_hand/set_l6_force
```

右手把 `left_hand` 换成 `right_hand`。

## O6 手模型

模型路径：

```text
开发资源/assets/components/hands/linkerhand_o6
```

左手 URDF：

```text
开发资源/assets/components/hands/linkerhand_o6/variants/left/hand.urdf
```

右手 URDF：

```text
开发资源/assets/components/hands/linkerhand_o6/variants/right/hand.urdf
```

## 主动关节

| 左手关节 | 含义 |
| --- | --- |
| `lh_thumb_cmc_yaw` | 拇指侧摆 |
| `lh_thumb_cmc_pitch` | 拇指弯曲 |
| `lh_index_mcp_pitch` | 食指弯曲 |
| `lh_middle_mcp_pitch` | 中指弯曲 |
| `lh_ring_mcp_pitch` | 无名指弯曲 |
| `lh_pinky_mcp_pitch` | 小指弯曲 |

右手前缀是 `rh_`。

> ⚠ **注意：控制数组的顺序与上表不同。** 话题里 6 个值的排列是
> `[拇指弯曲, 拇指侧摆, 食指, 中指, 无名指, 小指]` —— 拇指两路与上表
> （以及 URDF 关节名里的 yaw/pitch）**顺序相反**。这是 2026-09-16 在真机上
> 逐个通道实测确认的；按 URDF 顺序理解会把拇指两路搞反。

## mimic 关节

mimic 关节会跟随主动关节，不用单独控制。

| mimic 关节 | 跟随 |
| --- | --- |
| `thumb_ip` | 拇指弯曲关节 |
| `index_dip` | 食指弯曲关节 |
| `middle_dip` | 中指弯曲关节 |
| `ring_dip` | 无名指弯曲关节 |
| `pinky_dip` | 小指弯曲关节 |

## 控制话题

| 话题 | 类型 | 作用 | 数据长度 |
| --- | --- | --- | --- |
| `/robot1/left_hand/set_l6_joint` | `std_msgs/msg/UInt8MultiArray` | 左手位置 | 6 |
| `/robot1/left_hand/set_l6_speed` | `std_msgs/msg/UInt8MultiArray` | 左手速度 | 6 |
| `/robot1/left_hand/set_l6_force` | `std_msgs/msg/UInt8MultiArray` | 左手力矩 | 6 |
| `/robot1/right_hand/set_l6_joint` | `std_msgs/msg/UInt8MultiArray` | 右手位置 | 6 |
| `/robot1/right_hand/set_l6_speed` | `std_msgs/msg/UInt8MultiArray` | 右手速度 | 6 |
| `/robot1/right_hand/set_l6_force` | `std_msgs/msg/UInt8MultiArray` | 右手力矩 | 6 |

数组顺序（2026-09-16 真机实测确认）：

```text
[拇指弯曲, 拇指侧摆, 食指, 中指, 无名指, 小指]
```

**数值含义：255 = 完全张开，0 = 完全闭合**（六路一致）。

不同实物手指方向可能存在细微差异，第一次调试时用小数值逐个手指确认。

## 发布例子

张开左手（255 = 完全张开）：

```bash
ros2 topic pub --once /robot1/left_hand/set_l6_joint std_msgs/msg/UInt8MultiArray "{data: [255, 255, 255, 255, 255, 255]}"
```

设置左手速度：

```bash
ros2 topic pub --once /robot1/left_hand/set_l6_speed std_msgs/msg/UInt8MultiArray "{data: [80, 80, 80, 80, 80, 80]}"
```

设置左手力矩：

```bash
ros2 topic pub --once /robot1/left_hand/set_l6_force std_msgs/msg/UInt8MultiArray "{data: [60, 60, 60, 60, 60, 60]}"
```

闭合左手示例（0 = 完全闭合）：

```bash
ros2 topic pub --once /robot1/left_hand/set_l6_joint std_msgs/msg/UInt8MultiArray "{data: [0, 0, 0, 0, 0, 0]}"
```

## 调试建议

- 先设置较小速度和力矩。
- 先空载张开、闭合。
- 确认手指不会夹到线缆、夹具或桌面。
- 夹持物体时不要一开始就用 0（完全闭合），逐步收紧。
