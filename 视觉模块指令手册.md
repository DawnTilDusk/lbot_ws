# 视觉模块指令手册

适用本机：`/home/dawntildusk/lbot_ws`、ROS 2 Jazzy、Orbbec Gemini2、Conda `nut-yolo`、zsh。核对日期：2026-09-16。

## 1. 环境与模型

ROS 程序使用 `/usr/bin/python3`；YOLO 训练和独立预测使用 Conda Python。无需在 ROS 终端激活 Conda。以下 ROS 命令按 **zsh** 编写；使用 Bash 时，把所有 `setup.zsh` 换成 `setup.bash`，不要混用。

每个 ROS 终端先执行：

```zsh
cd /home/dawntildusk/lbot_ws
source /opt/ros/jazzy/setup.zsh
source /home/dawntildusk/orbbec_ws/install/setup.zsh
```

| 用途 | 仓库内权重 | 说明 |
| --- | --- | --- |
| 新视角螺母四分类 | `weights/nut_white_best.pt` | large / medium / small / white，实时预览显式指定此文件 |
| 铁杆两关键点 | `weights/needle_pose_best.pt` | Pose：0=tip 游离端端面中心，1=base 底座连接处，训练输入 960 |
| 原三分类业务 | `weights/nut_best.pt` | 现有抓取配置默认使用的模型 |

模型来源、SHA256 和指标见各自同名 `.json`。铁杆已接入只读实时预览，未接入机械臂运动业务。

GPU 检查（不需要 source ROS）：

```zsh
nvidia-smi
env -u PYTHONPATH -u PYTHONHOME /home/dawntildusk/miniconda3/envs/nut-yolo/bin/python -c 'import torch; print(torch.__version__); print("CUDA:", torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else "无可用 GPU")'
```

## 2. 启动相机驱动：终端 A

先执行第 1 节 ROS 环境命令。只启动一份驱动；如果已经有正常出图的相机驱动，直接使用它。

```zsh
ros2 launch orbbec_camera gemini2.launch.py \
  enable_color:=true enable_depth:=true depth_registration:=true \
  enable_point_cloud:=false enable_colored_point_cloud:=false
```

保持此终端运行，`Ctrl+C` 停止驱动。`depth_registration:=true` 用于彩色与深度对齐。

## 3. 确认出图：终端 B

先执行第 1 节 ROS 环境命令。

```zsh
ros2 topic info -v /camera/color/image_raw
ros2 topic hz /camera/color/image_raw
```

看到持续帧率后按 `Ctrl+C`，继续检查深度和内参：

```zsh
ros2 topic hz /camera/depth/image_raw
# 看到频率后 Ctrl+C，再执行下一行
ros2 topic echo /camera/color/camera_info --once --qos-reliability best_effort
```

话题存在不代表正在出图；彩色、深度都应持续发布。仅查看彩色画面：

```zsh
/usr/bin/python3 tools/camera_view.py --color-only
```

## 4. 螺母实时识别与坐标预览：终端 B

相机驱动保持运行；先执行第 1 节 ROS 环境命令。

```zsh
/usr/bin/python3 tools/nut_yolo_live.py \
  --model weights/nut_white_best.pt --device 0 --conf 0.5
```

- `s` 保存快照；`q` / ESC 退出。输出：`recordings/yolo_live/<时间戳>/`。
- 显示四类目标、框中心、相机 XYZ 和外参换算的机器人 XYZ；坐标单位米。
- `--scale 0.6` 只缩小显示窗口，不能靠它降低模型推理量。
- `--device cpu` 可排查 GPU 问题，但通常更慢；首次模型加载需要预热。
- 无窗口检查 15 秒：

```zsh
/usr/bin/python3 tools/nut_yolo_live.py \
  --model weights/nut_white_best.pt --device 0 --no-preview --duration 15
```

此脚本不发送机械臂运动命令。相机位置改变后，旧外参不再能保证机器人坐标准确，需要重新标定/核验。螺母孔中心深度也可能落在桌面，应检查取深度的位置。

### 铁杆实时关键点预览：只更换模型

```zsh
/usr/bin/python3 tools/nut_yolo_live.py \
  --model weights/needle_pose_best.pt --device 0 --conf 0.5
```

使用与螺母相同的相机启动和 ROS 环境。程序根据模型任务自动切换：铁杆 Pose 使用 960 输入、螺母 Detection 使用 640 输入，可用 `--imgsz` 覆盖。铁杆窗口显示框、红色 tip（游离端）、绿色 base（底座连接处）及两端像素坐标；关键点置信度低于 `--conf` 或越界时隐藏对应端点。快照 JSON 的 `keypoints` 按 tip/base 顺序保存 name/u/v/confidence/valid，无效端点坐标为 null。

当前入口仍使用同步彩色/深度和内参，因此相机启动方式保持不变。铁杆只输出二维像素坐标，不输出 XYZ；细杆深度可能取到底座或背景，尚未验证。原螺母坐标流程不变。过期预测仅显示在原检测帧，界面会标明非实时。

## 5. 采集新图片

相机驱动保持运行；先执行第 1 节 ROS 环境命令。此工具同样可以拍铁杆。

```zsh
# 手动拍照：空格/s 保存，a 切换自动，q 退出
/usr/bin/python3 tools/capture_nut_images.py --output /home/dawntildusk/nut_vision
```

或自动每 3 秒一张，共 100 张：

```zsh
/usr/bin/python3 tools/capture_nut_images.py \
  --output /home/dawntildusk/nut_vision --interval 3 --count 100
```

无窗口时追加 `--no-preview`。图片实际保存到终端打印的 `raw/<时间戳>/images/`，不是直接保存到 output 根目录。原始图片不画框，标签单独存储；训练不要使用画过框的预览图。

## 6. 铁杆离线预测与验证

以下命令不需要 ROS。使用已存在的测试图片路径，可直接执行；预测新图片时替换 `source=`。

```zsh
cd /home/dawntildusk/lbot_ws
env -u PYTHONPATH -u PYTHONHOME /home/dawntildusk/miniconda3/envs/nut-yolo/bin/yolo pose predict \
  model=weights/needle_pose_best.pt \
  source=/home/dawntildusk/nut_vision/datasets/needle_pose_20260916/images/test \
  imgsz=960 device=0 conf=0.5 save=True save_txt=True save_conf=True \
  project=/home/dawntildusk/nut_vision/runs name=needle_predict
```

输出标注图和预测标签到日志显示的目录。重复执行时 Ultralytics 会自动增加目录后缀。预测标签不是人工真值，不要未经核对直接加入训练集。

验证测试集：

```zsh
env -u PYTHONPATH -u PYTHONHOME /home/dawntildusk/miniconda3/envs/nut-yolo/bin/yolo pose val \
  model=weights/needle_pose_best.pt \
  data=/home/dawntildusk/nut_vision/datasets/needle_pose_20260916/data.yaml \
  split=test imgsz=960 batch=4 device=0 workers=2 \
  project=/home/dawntildusk/nut_vision/runs name=needle_test
```

已完成这一轮的预测对照：[预览](../nut_vision/runs/needle_pose_20260916/keypoint_preview/review.html)。圆圈是标注，十字是预测。测试 10 张，杆尖/根部平均误差约 4.43/3.25 原图像素；样本来自同次采集，不能替代独立场景测试。

## 7. 训练与追加训练

先停掉 GPU 实时预览，避免争抢显存。训练终端不需要 ROS。已有数据集：

| 数据集 | 配置 | 划分 |
| --- | --- | --- |
| 铁杆 Pose | `/home/dawntildusk/nut_vision/datasets/needle_pose_20260916/data.yaml` | 72 train / 20 val / 10 test，另 3 张待复核不参与训练 |
| 新视角四类螺母 | `/home/dawntildusk/nut_vision/datasets/nuts_newview_4class_20260916/data.yaml` | 381 train / 57 val / 38 test |

新数据应放入 `images/train`、`images/val`，对应同名标签放入 `labels/train`、`labels/val`。螺母 Detection 标签与铁杆 Pose 标签格式不同，保持两个数据集独立。铁杆标签每行 11 列：`class cx cy w h tip_x tip_y tip_v base_x base_y base_v`，坐标归一化；配置 `kpt_shape: [2, 3]`，关键点顺序不变。修改标签后删除该数据集 `labels/*.cache` 再训练。

### 7.1 在当前铁杆模型上追加训练（新优化器）

```zsh
cd /home/dawntildusk/lbot_ws
set -o pipefail
run_name="needle_pose_finetune_$(date +%Y%m%d_%H%M%S)"
env -u PYTHONPATH -u PYTHONHOME /home/dawntildusk/miniconda3/envs/nut-yolo/bin/yolo pose train \
  model=weights/needle_pose_best.pt \
  data=/home/dawntildusk/nut_vision/datasets/needle_pose_20260916/data.yaml \
  epochs=100 patience=25 imgsz=960 batch=4 device=0 workers=2 \
  optimizer=AdamW lr0=0.0003 lrf=0.01 \
  mosaic=0.2 close_mosaic=10 degrees=10 translate=0.1 scale=0.3 fliplr=0.5 flipud=0 \
  project=/home/dawntildusk/nut_vision/runs name="$run_name" \
  2>&1 | tee "/home/dawntildusk/nut_vision/${run_name}.log"
```

如需重跑最初的训练，使用本机已有初始模型：将 `model=` 换成 `/home/dawntildusk/nut_vision/datasets/needle_pose_20260916/yolo11n-pose.pt`，`lr0=0.001`。最初训练的完整参数和日志保存在该数据集目录的 `training_provenance.json`、`train_command.sh`、`train.log`。

### 7.2 在当前四类螺母模型上追加训练

```zsh
cd /home/dawntildusk/lbot_ws
set -o pipefail
run_name="nuts_finetune_$(date +%Y%m%d_%H%M%S)"
env -u PYTHONPATH -u PYTHONHOME /home/dawntildusk/miniconda3/envs/nut-yolo/bin/yolo detect train \
  model=weights/nut_white_best.pt \
  data=/home/dawntildusk/nut_vision/datasets/nuts_newview_4class_20260916/data.yaml \
  epochs=100 patience=25 imgsz=640 batch=8 device=0 workers=2 \
  optimizer=AdamW lr0=0.0003 \
  project=/home/dawntildusk/nut_vision/runs name="$run_name" \
  2>&1 | tee "/home/dawntildusk/nut_vision/${run_name}.log"
```

训练完成后查看输出目录：`weights/best.pt` 是验证集选出的最优权重，`weights/last.pt` 是最后一轮；`results.csv`、`results.png` 为训练指标。先验证并看预测图，再更新仓库权重。

上面的命令是从已有权重开始新一轮微调。只有**被中断且保留优化器状态**的 checkpoint 才适合 `resume=True`；已经正常结束、优化器被剥离的 `last.pt`，使用它作为 `model=` 开启新训练，不要盲目 resume。`epochs=100` 是上限，启用早停可能提前结束。

## 8. 常见故障

| 现象 | 可执行的检查 / 处理 |
| --- | --- |
| `ros2: command not found` / 找不到 `rclpy` | 执行第 1 节 source，ROS 脚本用 `/usr/bin/python3`；zsh 不要 source setup.bash |
| 找不到 `orbbec_camera` | `source /home/dawntildusk/orbbec_ws/install/setup.zsh`，再 `ros2 pkg prefix orbbec_camera` |
| 有话题但等不到图 | 第 3 节检查 publisher 和帧率，查看终端 A 驱动日志；确认 USB 连接、未重复启动驱动；多机时核对 `echo $ROS_DOMAIN_ID`，各端使用相同值 |
| 彩色正常但实时识别等帧 | 同时检查 `/camera/depth/image_raw` 和彩色内参，确认驱动开启深度对齐 |
| GPU 无法使用 | 第 1 节检查 CUDA；确认是 `nut-yolo` 环境；临时预览可用 `--device cpu` |
| 显存不足 | 关闭其他推理/训练；训练 batch 从 4 降为 2（或螺母从 8 降为 4） |
| 窗口打不开（SSH 无桌面） | 实时检测用 `--no-preview --duration 15`；拍照用 `--no-preview --interval 3 --count 10`；结果在本机桌面查看 |
| 显示三分类或模型类别不匹配 | 四类预览显式指定 `weights/nut_white_best.pt`；铁杆使用第 4 节 Pose 实时命令或第 6 节离线入口 |
| XYZ 不合理 | 检查对齐深度、内参和外参；重装相机后重新标定。铁杆目前只有二维关键点模型 |

核验范围：相机启动参数已对照本机驱动 launch 文件；环境加载、现有脚本 `--help`、模型复制哈希及铁杆离线预测已检查。编写手册时未重新启动实体相机，因此现场仍需按第 3 节确认真实出图。
